(function() {
  "use strict";

  angular.module("app").controller("jobCtrl", function($scope, $http) {
    $scope.setup = function() {
      $http.get("/jobs.json").then(function(response) {
        $scope.job = response.data;
      });
    }; // end of setup function

  });
}());





