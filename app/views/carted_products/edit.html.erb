<h1>Edit A Job</h1>

<%= form_tag "/carted_products/#{ @carted_product.id }", method: :patch do %>
  <div>
    <%= label_tag :priority %>
    <%=  text_field_tag :priority, @carted_product.priority %>

  <button type="submit" class="btn btn-default-outline">Submit</button>

  <% end %>